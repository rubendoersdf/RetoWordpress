

{% extends "base.twig" %}

{# {% extends "nav.twig" %} #}

{% block content %}


        
        
        <main class="flex items-center p-10 w-full h-full bg-white">
	<div class="border-t border-b pt-16 grid grid-cols-2 gap-8">
		<div class="flex flex-col justify-start">
			<div class="flex flex-col w-full object-cover h-5/6 justify-items-start border rounded-lg overflow-hidden"
				style="min-heigth:320px">
				<img class="w-full h-full object-content" src="{{post.imagen}}" alt='nike shoes' >
            </div>
			</div>
			<div class="flex flex-col">
				<div class="flex flex-col gap-4">
					<h1 class="capitalize text-4xl font-extrabold">{{post.title}}</h1>
					<h2 class="text-3xl">${{post.precio}}</h2>
					<p class="text-lg text-gray-500	">{{post.descripcion}}</p>
					<div class="flex items-center gap-4 my-6 cursor-pointer ">
						<div class="bg-blue-600 px-5 py-3 text-white rounded-lg w-2/4 text-center">Add to bag</div>
						<svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-red-400" viewBox="0 0 20 20"
							fill="currentColor">
							<path fill-rule="evenodd"
								d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z"
								clip-rule="evenodd" />
						</svg>
					</div>
				</div>
			</div>
		</div>
</main>

{% for r in post.relacionados %}

{{function('var_dump', r.28)}}

	 <div class="bg-white">
  <div class="max-w-2xl py-16 px-4 sm:py-24 sm:px-6 lg:max-w-7xl lg:px-8">

    <div class="mt-6 grid grid-cols-1 gap-y-10 gap-x-6 sm:grid-cols-2 lg:grid-cols-4 xl:gap-x-8">
    

      <div class="group relative">
        <div  class="w-full min-h-80 bg-gray-200 aspect-w-1 aspect-h-1 rounded-md overflow-hidden group-hover:opacity-75 lg:h-80 lg:aspect-none">
        <img src="{{r.imagen}}" alt="Front of men&#039;s Basic Tee in black."  class="w-full h-full object-center object-cover lg:w-full lg:h-full">
        </div>
        <div class="mt-4 flex justify-between">
          <div>
            <h3 class="text-sm text-gray-700">
              <a href="{{r.slug}}">
                <span aria-hidden="true" class="absolute inset-0"></span>
                {{r.title}}
              </a>
            </h3>
            <p class="mt-1 text-sm text-gray-500">{{r.descripcion}}</p>
          </div>
          <p class="text-sm font-medium text-gray-900">{{r.precio}}$</p>
        </div>
      </div>
      

      <!-- More products... -->
    </div>
  </div>
</div>

{% endfor %}


    
{% endblock %}

